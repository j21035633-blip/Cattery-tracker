# expo-router pulls a react-dom whose peer range is ahead of the react version
# the SDK pins, which makes a clean `npm install` fail on strict peer checks.
# The web target is not what this app ships, so the conflict is inert.
legacy-peer-deps=true
